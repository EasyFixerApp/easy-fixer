// * this is used to define any constants/configuration settings for the application

import { env } from "./env.js";
import { oas } from "./oas.js";

const config = {
  env,
  oas,
};

export default config;
