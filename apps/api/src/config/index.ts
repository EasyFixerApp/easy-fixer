import { env } from "./env.js";
import { oas } from "./oas.js";

/**
 * @description
 * This should be the only source of any constants/configuration settings for the application
 */

const config = {
  env,
  oas,
};

export default config;
