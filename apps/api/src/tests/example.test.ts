import { describe, expect, it } from "vitest";

describe("Basic functionality", () => {
  it("confirms that the test runner (Vitest) is working correctly", () => {
    expect(1 + 1).toBe(2);
  });
});
