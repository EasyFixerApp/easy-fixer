{
  "name": "easy-fixer-shared",
  "private": true,
  "version": "1.0.0",
  "description": "Shared TypeScript types and validation schemas for Easy Fixer applications",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "postinstall": "npm run build",
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint . --ext .ts",
    "lint:fix": "eslint . --ext .ts --fix",
    "check:type": "tsc --noEmit"
  },
  "devDependencies": {
    "zod": "^3.22.0"
  },
  "peerDependencies": {
    "zod": "^3.22.0"
  }
}
